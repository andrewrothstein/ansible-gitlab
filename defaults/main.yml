---
# https://packages.gitlab.com/gitlab/
gitlab_mirror: https://packages.gitlab.com
gitlab_ver: '12.7.5'
gitlab_dist: ce

gitlab_key_url: https://packages.gitlab.com/gpg.key

gitlab_os_dep_pkgs:
  - openssh-server
  - postfix

gitlab_download_timeout_seconds: 600
