---
# defaults file for gitlab
gitlab_ce_ver : '8.3.2-ce.0'

gitlab_pkg_mirror : 'https://packages.gitlab.com/install/repositories/gitlab'

gitlab_backups_dir : '/var/opt/gitlab/backups'

gitlab_backups_vault : /mnt/blade-share/gitlab-backups

gitlab_ts :  1451430631

gitlab_install : True
gitlab_restore : False
gitlab_backup : False
gitlab_vault : False
